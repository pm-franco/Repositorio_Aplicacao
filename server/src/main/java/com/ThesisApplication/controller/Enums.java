package com.ThesisApplication.controller;

public class Enums {

    public enum Role {
        student,
        researcher,
        admin
    }

    public enum Secrets {
        researcherPassword,
        adminPassword,
        delete
    }
}
